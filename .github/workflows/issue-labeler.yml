name: "Issue Labeler"
on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: github/issue-labeler@master #May not be the latest version
      with:
        repo-token: "${{ secrets.BOT_TOKEN }}"
        configuration-path: .github/labeler.yml
