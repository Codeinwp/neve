describe('Widgets', function () {
	before(function () {
		cy.loginWithRequest();
	});

	it('Adds a new Widget', function () {
		/** TODO */
	});

	it('Reorders widgets', function () {
		/** todo */
	});
});
