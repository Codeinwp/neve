build:
# Transpile SCSS
- 'sass'
# Concatenate Customizer Controls CSS Files into one || todo: review this on customizer audit
- 'concat'
# RTL CSS
- 'rtlcss'
# Minify CSS
- 'cssmin'
# Add TextDomain to Composer Dependencies.
- 'addtextdomain:composerDeps'
# Add TextDomain to PHP Files Dependencies.
- 'newer:addtextdomain:main'

# Version:
#         1. grunt version::patch
#         2. grunt generate
#         3. commit & push for version update
#         4. zip & upload to wordpress.org
