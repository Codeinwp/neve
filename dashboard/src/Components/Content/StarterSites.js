/* global neveDash */
const {__} = wp.i18n;
const {Fragment} = wp.element;

const StarterSites = (props) => {
  return (
    <Fragment>
      Starter Sites
    </Fragment>
  );
};

export default StarterSites;
