#!/usr/bin/env bash
aws s3 cp s3://$VISUAL_REGRESSION_BUCKET/$VISUAL_REGRESSION_BASELINE.zip  $VISUAL_REGRESSION_ROOT/$VISUAL_REGRESSION_BASELINE.zip
cd $VISUAL_REGRESSION_ROOT
unzip $VISUAL_REGRESSION_BASELINE.zip -d ./snapshots/
rm -rf $VISUAL_REGRESSION_BASELINE.zip
