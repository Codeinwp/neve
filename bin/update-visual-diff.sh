#!/usr/bin/env bash
TMP_PATH=$(pwd)
cd $VISUAL_REGRESSION_ROOT/snapshots
zip -r  $TMP_PATH/$VISUAL_REGRESSION_ROOT/$VISUAL_REGRESSION_BASELINE.zip . --exclude "*.DS_Store" --exclude "*__diff_output__*"
cd $TMP_PATH
aws s3 cp $VISUAL_REGRESSION_ROOT/$VISUAL_REGRESSION_BASELINE.zip s3://$VISUAL_REGRESSION_BUCKET/$VISUAL_REGRESSION_BASELINE.zip
rm -rf cd $TMP_PATH/$VISUAL_REGRESSION_ROOT/$VISUAL_REGRESSION_BASELINE.zip
