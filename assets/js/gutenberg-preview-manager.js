/* global neveGutenbergHelper */

(function ( $ ) {
	$.neveGutenberg = {
		init: function () {
			console.log( neveGutenbergHelper );
		},

	};
})( jQuery );

jQuery( document ).ready( function () {
	jQuery.neveGutenberg.init();
} );
