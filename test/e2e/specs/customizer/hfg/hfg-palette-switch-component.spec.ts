import { test, expect } from '@playwright/test';
import { loadData, setCustomizeSettings } from '../../../utils';

test.describe('Palette Switch component', function () {
	// test.beforeAll(async ({ request, baseURL }) => {
	// 	await loadData(
	// 		'./customizer/hfg/data/hfg-palette-switch-component.json'
	// 	).then(async (data) => {
	// 		await setCustomizeSettings(data, { request, baseURL });
	// 	});
	// });
	//
	// test('Changes the color palette by clicking', async ({ page }) => {
	// });
});
